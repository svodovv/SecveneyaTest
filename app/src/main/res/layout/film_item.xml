<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="4dp"
    android:orientation="vertical"
    android:id="@+id/llFilm">


    <ImageView
        android:id="@+id/ivFilm"
        android:layout_width="match_parent"
        android:layout_height="222dp"
        android:layout_gravity="center"
        android:visibility="visible"
        android:scaleType="fitXY"
        android:adjustViewBounds="true"/>

    <FrameLayout
        android:id="@+id/flError"
        android:layout_width="match_parent"
        android:layout_height="222dp"
        android:layout_gravity="center"
        android:background="@drawable/rounder_corners_error_film_iv"
        android:visibility="gone">

        <ImageView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_gravity="center"
            android:scaleType="centerInside"
            android:src="@drawable/ic_error" />
    </FrameLayout>

    <TextView
        android:id="@+id/tvFilmName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        style="@style/TextViewBodyLarge"
        android:layout_marginBottom="20dp"
        tools:text="Очень длинный длинный текст, Очень длинный длинный текст, Очень длинный длинный текст,."
        />
</LinearLayout>